import * as React from 'react';
import { IconButton } from '@material-ui/core';

const StyledExaminationStatusesWithIcons = () => {
  return (
    <React.Fragment>
      <IconButton icon='camera'></IconButton>
      <IconButton icon='medical-file'></IconButton>
    </React.Fragment>
  );
};

export default StyledExaminationStatusesWithIcons;
