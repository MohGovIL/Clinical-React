/**
 * @author Yuriy Gershem yuriyge@matrix.co.il
 * emptyArrayAll - function returns a empty array for lists
 */

export const emptyArrayAll = (name) => {
    return [{
        code: 0,
        name: name
    }]
};
