import styled from "styled-components";
import MuiDialogTitle from "@material-ui/core/DialogTitle";

export const StyledMuiDialogTitle = styled(MuiDialogTitle)`
 .MuiIconButton-root {
   left: 0px; //or right in ltr mode
   right: 0px;
   top: 8px;
   position: absolute;
 }
`;
