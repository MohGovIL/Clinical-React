import React from 'react';
import HeaderIconStyle from "./HeaderIconStyle";
const HeaderIcon = ({onClick}) => {
    return (
        <HeaderIconStyle onClick={onClick}/>
    );
};

export default HeaderIcon;
